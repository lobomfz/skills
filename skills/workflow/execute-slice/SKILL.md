---
name: execute-slice
description: "Implement the agreed slice. Use when the user invokes $execute-slice or wants to implement a slice agreed via slice-prd."
disable-model-invocation: true
---

Update the PRD only with new facts about the feature's end goal that came up in the grilling. The PRD describes the end state, never the path to it — do not list slices, build a roadmap, or use names from the implementation. Replace contradicting decisions.

Load every relevant skill before starting.

Implement the slice the user just agreed on, working autonomously without pausing for approval.

After finishing, mark the covered user stories as done and tell the user how to validate the delivered slice. Do not propose the next slice.
